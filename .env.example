# Arch Linux VS Code Container Configuration
# Copy this file to .env and customize as needed

# ============================================================================
# User Configuration
# ============================================================================
# User ID (should match your host user ID to avoid permission issues)
PUID=1000

# Group ID (should match your host group ID to avoid permission issues)
PGID=1000

# Username for the container user (default: developer)
# This is the user that will run VS Code and all processes
USERNAME=developer

# ============================================================================
# Workspace Configuration
# ============================================================================
# Workspace directory inside the container (default: /workspace)
WORKSPACE_DIR=/workspace

# Default folder VS Code should open (default: /workspace)
# This is the folder that VS Code will open when you access the web interface
VSCODE_DEFAULT_FOLDER=/workspace

# ============================================================================
# VS Code Server Configuration
# ============================================================================
# Bind address for VS Code server (default: 0.0.0.0)
# Use 127.0.0.1 for localhost-only access
VSCODE_HOST=0.0.0.0

# Port for VS Code server (default: 8080)
VSCODE_PORT=8080

# Connection token for authentication (default: empty = no auth)
# If set, access VS Code at: http://localhost:8080/?tkn=YOUR_TOKEN
VSCODE_CONNECTION_TOKEN=

# UNIX socket path (optional, default: empty = use TCP)
# If set, VS Code will listen on a socket instead of TCP
VSCODE_SOCKET_PATH=

# Auto-accept VS Code server license terms (default: true)
VSCODE_ACCEPT_LICENSE=true

# Enable verbose logging (default: false)
VSCODE_VERBOSE=false

# Log level (default: info)
# Options: trace, debug, info, warn, error, critical, off
VSCODE_LOG_LEVEL=info

# ============================================================================
# VS Code Data Directories
# ============================================================================
# These paths control where VS Code stores its data
# Defaults are under $HOME/.config/arch-vscode/
# Override these if you need a custom directory layout

# Root config directory (default: $HOME/.config/arch-vscode)
# VSCODE_CONFIG_ROOT=$HOME/.config/arch-vscode

# User data directory for settings and preferences (default: $VSCODE_CONFIG_ROOT/user-data)
# VSCODE_USER_DATA_DIR=$VSCODE_CONFIG_ROOT/user-data

# Extensions directory (default: $VSCODE_CONFIG_ROOT/extensions)
# VSCODE_EXTENSIONS_DIR=$VSCODE_CONFIG_ROOT/extensions

# Server data directory (default: $VSCODE_CONFIG_ROOT/server-data)
# VSCODE_SERVER_DATA_DIR=$VSCODE_CONFIG_ROOT/server-data

# CLI data directory (default: $VSCODE_CONFIG_ROOT/cli-data)
# VSCODE_CLI_DATA_DIR=$VSCODE_CONFIG_ROOT/cli-data

# ============================================================================
# XDG Base Directory Configuration
# ============================================================================
# XDG base directories (optional, defaults shown)
# These control where config and data files are stored system-wide
# XDG_CONFIG_HOME=$HOME/.config
# XDG_DATA_HOME=$HOME/.local/share

# ============================================================================
# Package Installation
# ============================================================================
# Space-separated list of Arch Linux packages to install on container start
# Packages are installed via pacman/yay
# The package database is automatically synced before installation
# Examples:
#   EXTRA_PACKAGES="vim neovim python python-pip"
#   EXTRA_PACKAGES="git-lfs docker kubectl terraform"
EXTRA_PACKAGES=

# Space-separated list of npm packages to install globally on container start
# If npm is not available, nodejs and npm will be installed automatically
# Examples:
#   NPM_PACKAGES="typescript eslint prettier"
#   NPM_PACKAGES="@anthropic-ai/claude-code @google/gemini-cli"
NPM_PACKAGES=

# ============================================================================
# System Configuration
# ============================================================================
# Enable automatic system updates (default: false)
# When enabled, system updates run via cron at 2 AM daily
AUTO_UPDATE=false

# Timezone (default: UTC)
# Examples: America/New_York, Europe/London, Asia/Tokyo
TZ=UTC
